language: julia
os:
  - linux
  - osx
julia:
  - 0.6
# - nightly
notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia -e 'Pkg.add("Expokit")'
  - julia -e 'Pkg.add("LightGraphs")'
  - julia -e 'Pkg.clone("https://github.com/QuantumWalks/QuantumWalk.jl.git")'
  - julia -e 'Pkg.build("QuantumWalk")'
  - julia -e 'Pkg.test("QuantumWalk"; coverage=true)'
after_success:
  - julia --depwarn=no -e 'cd(Pkg.dir("QuantumWalk")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(process_folder())'
  - julia -e 'Pkg.add("Documenter")'
  - julia -e 'cd(Pkg.dir("QuantumWalk")); include(joinpath("docs", "make.jl"))'
